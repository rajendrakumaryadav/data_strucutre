name: Building Project...

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Running build!"
      - uses: actions/checkout@v2
      - name: make
        run: make
      - name: make run
        run: make run
